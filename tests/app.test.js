const request = require('supertest');


describe('POST', () => {
  it('/products creates a product', async () => {
        expect(2).toEqual(2);
    
  });
});

describe('GET', () => {
  it('/products creates a product', async () => {
        expect(2).toEqual(2);
    
  });
});

describe('DELETE', () => {
  it('/products creates a product', async () => {
        expect(2).toEqual(2);
    
  });
});

describe('PUT', () => {
  it('/products creates a product', async () => {
        expect(2).toEqual(2);
    
  });
});
describe('PATCH', () => {
  it('/products creates a product', async () => {
        expect(2).toEqual(2);
    
  });
});
